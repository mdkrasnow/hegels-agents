# =============================================================================
# Environment Variables Template for Hegels Agents
# =============================================================================
#
# ⚠️  SECURITY WARNING:
# - Copy this template to .env: cp .env.template .env
# - Edit .env with your actual values
# - NEVER commit .env to version control
# - .env is already in .gitignore for protection
#
# =============================================================================

# =============================================================================
# API Keys and Authentication
# =============================================================================

# Google Gemini API Key
# Get your API key from: https://makersuite.google.com/app/apikey
# Required for all LLM operations and embedding generation
GEMINI_API_KEY=your_api_key_here

# =============================================================================
# Database Configuration
# =============================================================================

# Supabase Database URL (or PostgreSQL with pgvector)
# Format: postgresql://username:password@host:port/database
# Required for Phase 1b and later (corpus storage and retrieval)
SUPABASE_DB_URL=postgresql://user:password@host:5432/database

# Database Pool Configuration (Optional)
# Adjust based on your application load and worker count
DB_POOL_SIZE=10

# =============================================================================
# Performance Configuration (Optional)
# =============================================================================

# Embedding cache size in MB (default: 500)
# Increase for large corpora, decrease for memory-constrained environments
EMBEDDING_CACHE_SIZE_MB=500

# API Rate Limiting (requests per minute)
# Adjust based on your Gemini API quota
API_RATE_LIMIT_RPM=100

# =============================================================================
# Development Configuration (Optional)
# =============================================================================

# Environment mode (development, staging, production)
APP_ENVIRONMENT=development

# Enable debug logging (true/false)
DEBUG_LOGGING=false

# Enable experiment tracking (true/false)
ENABLE_EXPERIMENT_TRACKING=true

# =============================================================================
# Security Best Practices:
# =============================================================================
#
# 1. API Key Security:
#    - Use API keys with minimal required permissions
#    - Rotate API keys regularly (recommended: every 90 days)
#    - Never share API keys in chat, email, or public repositories
#
# 2. Database Security:
#    - Use strong passwords (min 16 chars, mixed case, numbers, symbols)
#    - Enable SSL/TLS for all database connections (sslmode=require)
#    - Configure IP allowlisting for database access
#    - Use database users with minimal required privileges
#
# 3. Production Deployment:
#    - Use secret management tools (AWS Secrets Manager, HashiCorp Vault)
#    - Implement key rotation procedures
#    - Monitor for credential exposure in logs and error messages
#    - Set up regular security audits of environment variables
#
# 4. Development Workflow:
#    - Review .gitignore before each commit
#    - Use environment-specific .env files (.env.local, .env.staging)
#    - Validate that secrets are properly excluded from version control
#
# =============================================================================